// MISSÃO FINAL M12 · JAVASCRIPT MODERNO
// Complete as quatro funções e execute os 8 casos no Laboratório.

const criarEtiqueta = (produto) => {
  // Retorne: "P01 - TECLADO"
};

function atualizarProduto(produto, alteracoes) {
  // Crie um novo objeto com spread. Alterações devem prevalecer.
}

function somarValores(...valores) {
  // Some qualquer quantidade de números. Sem números, devolva 0.
}

function criarResumoUsuario({ nome = "Visitante", endereco } = {}) {
  // Use optional chaining e ?? para cidade ausente.
  // Retorne: { nome, cidade }
}

